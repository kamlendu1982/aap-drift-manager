# Machine Credentials
# Note: Actual secrets (passwords, SSH keys) are NOT stored here
# They should be managed separately via AAP UI or vault
name: Machine Credentials
description: SSH credentials for connecting to managed hosts
organization: Default
credential_type: Machine
# inputs are intentionally omitted - secrets managed separately
